# 1. Make sure everything on develop is committed
git status          # (Check if anything is uncommitted)
git add .
git commit -m "Your commit message"

# 2. Switch to the main branch
git checkout main

# 3. Merge changes from develop into main
git merge develop

# 4. Push the updated main branch to GitHub
git push origin main


=======================
simple daily work;-
git status
git add .
git commit -m "your commit message"
git push origin develop


=============